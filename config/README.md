This directory should contain any files related to the configuration of the application.

Currently, this directory hosts a `.env` file on initialization. Once input is set by the user, it should be formatted like the following:

```python
PORT=Port Number
DB_URI="URI for database"
```